# 智能背单词计划安排器

当前开发版本：v0.3.0 - 完整智能背词 Beta 版

这是一个本地离线 Web 应用，核心定位是“真正可背词的软件 + 动态计划中枢 + AI 规划接口”。用户可以导入真实词表，生成绑定具体单词的新学任务，逐词学习和复习，并让本地调度逻辑根据词库缺口、学习欠缺和复习压力持续重排计划。

v0.3.0 正在从 v0.2.1 技术基线升级，目标是补齐结构化目标、自然语言目标建议、阶段计划、词书匹配、周月复盘、词条历史、AI 安全降级和 v0.3.0 备份迁移闭环。已冻结的产品边界见 `docs/v0.3.0_product_scope.md` 和 `docs/v0.3.0_acceptance.md`。

## 技术栈

- React + TypeScript + Vite
- Dexie / IndexedDB
- Vitest
- 本地规则 AIPlanningService，不默认接入真实外部 API

## 启动方式

```bash
npm.cmd install
npm.cmd run dev
```

运行测试和构建：

```bash
npm.cmd test
npm.cmd run build
```

## 主要使用流程

1. 在“词书管理”载入演示词表，或导入 CSV/JSON 词表。
2. 在“目标设置”填写结构化目标，或输入自然语言目标并生成本地 AI mock 建议。
3. 保存目标后，系统生成绑定具体 `wordId` 的每日新词任务。
4. 在“今日学习”逐词标记“已学习 / 已掌握 / 暂时跳过 / 今日未完成”。
5. 到期复习时先查看单词，再查看释义，并选择“不认识 / 模糊 / 认识 / 很熟悉 / 未完成”。
6. 逐词操作只影响当前词条；用户点击“结束今日学习并安排未完成任务”后，剩余未处理任务才会结算并重排。
7. 在“计划”和“统计”查看长期覆盖、月/周/日计划、欠缺任务、逾期复习和调整日志。
8. 在“数据管理”导出或导入完整备份。v0.1.0 备份会兼容导入为历史数量记录。

## 导入词表格式

CSV 字段：

```csv
word,meaning,book_name,level,tags
example,例子,My Word Book,B1,noun
adapt,适应,My Word Book,B1,verb;core
```

JSON 顶层应为数组，每项至少包含 `word` 字段，可选 `meaning`、`book_name`、`level`、`tags`。

同一单词来自多本词书时，系统只创建一个学习对象，但保留多个来源词书。

## v0.2.1 已实现并必须承接

- 具体单词级新词任务。
- 具体单词级复习任务、反馈和复习历史。
- 单个词条结果保存与当日任务结算分离，避免当天正常未处理任务被提前计为欠缺或逾期。
- 主动结算今日未处理任务与跨日自动结算历史开放任务。
- 词库供给缺口与学习欠缺任务分开统计。
- 导入新词书后补足词库缺口并重排。
- 自然语言目标入口与 AI mock 建议。
- Dexie v2 数据结构和 v0.1.0 备份兼容迁移。
- 默认使用浏览器本地时区，目标中保存计划时区。

## v0.3.0 计划完成

- 结构化目标字段补齐和目标版本历史。
- 自然语言目标建议、影响预览和确认后应用。
- 词书推荐状态、导入状态、启用状态和补缺候选状态。
- 可解释的具体学习范围选择优先级。
- 今日任务工作台、词条详情、学习历史和重点遗忘词。
- 长期、阶段、月度、周度和每日计划联动。
- 周度复盘、月度复盘和风险数量解释。
- AI 接口层、本地规则模式、结构校验和失败降级。
- Dexie v3 数据迁移和 v0.3.0 完整备份恢复。

## 当前未实现或不在本轮范围

- 真实 AI API 调用。
- 发音、例句、拼写训练和听力训练。
- 登录、云同步和多端同步。
- 自动抓取或内置商业完整词书。
- v0.3.0 完成验证前不会创建正式 Release；若当前环境无法创建 GitHub Release，将生成 `docs/v0.3.0_release_notes.md` 供手动发布。
